- 重构全景图生成弹窗 UI（web/src/components/canvas/canvas-panorama-config-modal.tsx）：重新设计层级——顶部模式选择大卡（AI 生成带"推荐"徽章）、左侧编号步骤（1 源图/参考图 + 2 场景描述）、智能比例+复制描述+完整 prompt 收纳进"高级选项"折叠、右侧仅保留全景类型卡片（球形带推荐徽章+选中 check）和输出比例预览（aspect-ratio 可视化）、底部主操作加大 h-11 + ⌘↵ 快捷键（含全局 keydown 监听）。图片全部加 onLoad/onError 加载态（animate-pulse 骨架 + 加载失败兜底），主区和选择器弹层都覆盖。对外 props/导出类型/prompt 拼装逻辑不变，tsc --noEmit 零错误。
